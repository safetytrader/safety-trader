{`leading-5 ${l.includes("✅") || l.includes("👷") || l.includes("📋") || l.includes("📎") ? "text-emerald-400" : l.includes("❌") ? "text-red-400" : "text-slate-300"}`}>{l}</motion.div>)}{imp.extracting && <motion.div className="flex items-center gap-2 text-slate-400 mt-1"><motion.div className="w-3 h-3 border border-blue-400 border-t-transparent rounded-full animate-spin" />In corso…</motion.div>}</motion.div>}
<motion.div className="bg-white rounded-xl border border-slate-100 shadow-sm overflow-hidden"><motion.div className="px-5 py-3 border-b border-slate-100 flex items-center justify-between"><span className="font-semibold text-slate-700 text-sm"></motion.div><motion.div className="divide-y divide-slate-50 max-h-48 overflow-y-auto">{imp.uploadedFiles.map((f, i) => <motion.div key={i} className="px-5 py-2 flex items-center gap-3"><span>{f.type === "application/pdf" ? "📄" : "🖼️"}</span><motion.div className="flex-1 min-w-0"><motion.div className="text-sm text-slate-700 truncate">{f.name}</motion.div><motion.div className="text-xs text-slate-400">{(f.size / 1024).toFixed(0)} KB</motion.div></motion.div></motion.div>)}</motion.div></motion.div>}
